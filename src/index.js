// import cipher from './cipher.js';

// console.log(cipher);

// _offset_

const mensajeInput = document.getElementById('string');
mensajeInput.addEventListener('input',() => {
  mensajeInput.value = mensajeInput.value.toUpperCase();
});

const mensajeInput2= document.getElementById('string2');
mensajeInput2.addEventListener('input',() => {
  mensajeInput2.value = mensajeInput2.value.toUpperCase();
});


  
